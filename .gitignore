node_modules/
dist/
__pycache__/
*.pyc
*.db
# El diario de SQLite queda suelto si el servidor local se corta a media
# escritura. Es estado de la maquina, no del proyecto.
*.db-journal
backups/
.env
backend/respaldos_estado.json

# Estado local que no viaja: usuarios y secreto de sesion (fuera de Docker)
compartido/
# Entorno virtual de Python para correr las pruebas
.venv/
# Entorno para correr las pruebas sin Docker (pytest + sqlalchemy).
.venv-pruebas/
